@using TiendaElectronica.Data;
@model CategoriaModel
@{
    ViewData["Title"] = "Editar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<hr />
<div class="container-fluid ">
    <div class="col-8">
        <div class="card">
            <div class="card-header">
                Editar Categoria
            </div>
            <input asp-for="Id" type="hidden" />
            <div class="card-body">
                <form asp-action="Editar" asp-controller="Categorias" method="post">
                    <div class="mb-3">
                        <label class="form-label">Nombre</label>
                        <input asp-for="Descripcion" type="text" class="form-control" />
                        <span asp-validation-for="Descripcion" class="text-danger"> </span>
                    </div>

                    <button type="submit" class="btn btn-dark">Registrar</button>
                    <a asp-action="Listar" class="btn btn-dark" asp-controller="Categorias">Volver a lista</a>
                </form>
            </div>
        </div>
    </div>

</div>